# Copyright Ultralytics https://ultralytics.com, licensed under GNU GPL v3.0
# COCO128 dataset https://www.kaggle.com/ultralytics/coco128 (first 128 images from COCO train2017)
# YOLOv5 🚀 example usage: python train.py --data coco128.yaml
# parent
# ├── yolov5
# └── datasets
#     └── coco128  ← downloads here


# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: dataset  # dataset root dir
train: train/images # train images (relative to 'path') 128 images
val: valid/images  # val images (relative to 'path') 128 images
test:  train/images # test images (optional)

# Classes
nc: 7  # number of classes
names: ['speed limit 30', 'speed limit 50', 'no overtaking',
          'priority road', 'give way', 'stop', 'keep right']  # class names

